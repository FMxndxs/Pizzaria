# Docs-B4: API spec — modelo de recursos + auth

## Business Outcome
Um desenvolvedor externo sabe como autenticar e entende o modelo de recursos da API.

## Scope
- `guides/api-spec.md` seções 1 (auth Bearer + roles + obter token) e 2 (modelo de recursos: Order, OrderItem, OrderStatus, FulfillmentType, formato de erro padrão).
- Tipos batem com `src/lib/orders/schemas.ts` e `src/types/index.ts`.

## Acceptance Criteria
- [x] Seção de autenticação com exemplo de request/response.
- [x] Tabela de roles e escopos.
- [x] Modelo completo de `Order` e `OrderItem`.
- [x] Formato de erro padrão documentado.
- [x] Docs atualizados.

## Status
`done`

## Commits
- `red:` — · `green:` 1001afc · `blue:` fcc159c · `document:` (este)
